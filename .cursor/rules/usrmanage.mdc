---
description: Follow usrmanage AGENTS.md product locks, testing, and release invariants
alwaysApply: true
---

# usrmanage

Read and follow [AGENTS.md](AGENTS.md) for this repository.

Key invariants: OpenWrt-default password policy until Save; passwords never on argv/logs/MCP; LuCI `expect: { '': {…} }`; no unquoted `$*` into privileged CLI; prefer flock for locks.

Testing: `./scripts/smoke-host.sh` (PR CI). Lab/Playwright: [docs/developer/testing.md](docs/developer/testing.md).

Release: third-octet `PKG_VERSION`, tag `v*`, no secrets in git — [docs/release.md](docs/release.md).
